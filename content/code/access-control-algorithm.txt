FUNCTION ProcessRequest(c, a, q, P)
  INPUT:
    client credentials c
    requested mode of access a
    quad pattern query q
    list of policies P
  OUTPUT:
    query results R
R = {}
FOREACH p in P
  IF AllowedAccess(c, a, q, p) == true    
    R = ExecuteQuery(R, q)
RETURN R
