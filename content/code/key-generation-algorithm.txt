FUNCTION CreateAccessKeys(Q, P)
  INPUT:
    list of quads Q
    list of policies P
  OUTPUT:
    hashmap of quads to policies and keys QPK
QPK<q,item<p,k>> = MapInitialize()
FOREACH q in Q
  FOREACH p in P
    key k = null
    k = GenerateKey(q,p)
    QPK = AddKey(QPK, q, p, k)
RETURN QPK